3/10-3/19:
Finished HELM model.py
    After months of learning ML theory + planning this architecture, the script has been written

3/19-3/20:
Finished prepare_data.py & tokenized 10B tokens
    take 10B tokens from dataset "HuggingFaceFW/fineweb-edu/sample-10BT"
    tokenized via tokenizer from "answerdotai/ModernBERT-base" 
    learned about python generators (very similar to iterators)
    created checkpoing alg to save shards of tokens every so often
    created failsafes to ensure tokens don't get repeated twice
    tokens are perfectly padded to 1024, 2048, 4096
    ran on kaggle

3/20-3/22:
Finished the SpanMLMCollator.py
    takes the tokens from prepare_data.py and properly apply MLM masking + span masking

3/21-3/24:
Finished Phase 1: parallel_hardware_trainer.py
    created parallel_hardware_trainer.py, which includes all training stuff
    HardwareConfig, MLMDataConfig, MLMDataStrategy, HardwareDriver, train_worker defined
    runs all kaggle hardwares


3/24:
Retokenized everything
    accidently deleted portions of my dataset permanently
    overlapping may have occured
    created proper validation sets for all seq lengths

3/24-3/26:
Finished Phase 2: Fixing Model.py + adding curriculum learning
    fixed minor issues in the trainer program
    added curriculum loading
    define hardware profiles
    added AMP (automatic mixed precision) gradient scaling so I can run more and faster
    added gradient checkpointing (cuda only), higher batch count 
    fixed model.py with the head selection strategy / other design choices for different hardwares

3/27-3/29:
Starting Phase 3: Adding Checkpointing System:
    Added new config: CheckpointConfig (includes checkpointing info)
    Defined / Finalized training_state.json format
    Added to create the init, parsing .jsons from HF, saving, etc
    Added load checkpointing (sorta vibe-coded)

3/30:
Continuing Phase 3: Finished Checkpoint Driver Class (added save_checkpoint)
    Updates metadata
    Updates training_state.json
    Saves the model (hardware agnostic)
    pings the sidecar via UPLOADER_REQUEST.json adjustment

3/31 (2 Hours):
Continuing Phase 3: Included Exclusive Attention Toggle (in config as well):
The Mathematical Formula:
    If y_i is the output of standard self-attention for position i, and v_i is the self-value vector, 
    the exclusive output z_i is defined by subtracting the projection of y_i onto v_i:
    z_i = y_i - proj(y onto v)
    By removing this component, z_i becomes mathematically orthogonal to v_i, ensuring the attention layer 
    only passes on information that the token didn't already "know" about itself.
 
Rethought the excessive normalization in the router:
    normalizing before the sigmoid selection reduces the magnitude / volume is BAD
    reason: sigmoid doesn't care about the other values when calculating it's sigmoid (softmax does (via the division of the summation))
    ngpt: Angles matter, volume (module / magnitude) is dangerous.
    router (outside): Volume = Confidence
    we are applying a sigmoid THRESHOLD, so like we need this to not be normalized
    removed all normalization in the router that doesn't deal with the hidden states directly 
    (so removed latent / classification score normalization)

Created this log to help me document what is happening

4/4 (2 Hours):
    Finish Sidecar 
    Updated Main
    Added Back Emojis to messeges: Lwk it looks AI generated (I mean this idea is AI inspired), but man does it make debugging easier 

4/7 (3 Hours):
    Attempting to implement checkpointing logic into train_worker
    PROBLEM: checkpoints and metadat aren't correctly synced if I deleted something, leading to errors:
    SOLUTION: Delete metadata logic. Each checkpoint will each contain all the information inside each checkpoint value
    Updated Checkpoint Driver to remove Metadata Logic
    Started Fixing train_worker

4/8 (6 Hours):
    Fixed train_worker - now successfully uploads the files to the HF Hub
    Created Repo to ensure my stuff doesn't get lost
    Created "Upload Queue" structure to allow for multiple upload requests if training the model was faster than the uploading
    Completed Phase 3 - Checkpoint trainer
    !pkill -9 -f python - good for killing zombie tpu processes where you click the pause button instead of letting it end naturally
    I notice that our router keeps collapsing, our Aux Loss (only using the 4 same heads im guessing). Maybe up the sparsity loss / get rid of warm up???

4/9 (1 Hour):
    Finalized phase_03 - now checkpoints pretty good
    Realized I forgor to normalize the weights before the optimizer, and fixed that

4/11 (3 Hours):
    Fixed Dependencies to ensure hf-hub works
    New Dependices scripts exclusively for h100 and g4
    Testing other HW profiles (for size 1024)
        - v5e-1 tpu (colab) ✅ 16
        - v6e-1 tpu (colab) ✅ 16
        - t4 gpu (colab) ✅ 4
        - l4 gpu (colab) ✅ 8
        - a100 gpu (colab) ✅ (32, False = 3.5s) (64, True = 4.2s)
        - g4 gpu (colab) ✅ (32, False = 2.75s) (64, True = 3.5s)
        - h100 gpu (colab) ✅  (32, False = 2.9s) (64, True = 3.5s)

        - v5e-8 tpu (kaggle) ✅ 16
        - p100 tpu (kaggle) ✅ (8, True = 45s) (16, True = 45s, more risky)
        - t4*2 gpu (kaggle) ✅ (4, True = 17s) (8, True = 17s) (4, False = 15s)
    
    Fixed 0th step intialization for multi gpu - unwrapped the model then called _init_weights

4/12 (9 Hours + Dilly Dallying):
    Started to work on Telemetrics
    Learning what wandb is
    Vibecoded a bunch of the logic (Im too tired for this, but lwk not too bad)
    Validation Loop happens every 500 steps
    The thing sort of works
    Thinking about a new project relating to RAGs...

4/13 (6 Hours):
    Finished Telemetrics
    Created Logic to Resume from the last wandb run (i.e stopped at 141, wandb now goes back to 100 and rewrites)
    Actually just learned its a beta-testing feature that I can't use, so I added does_resume_from_work bool
    Added back the sigmoid scores
    Added back l_i_weights
    Um Tangled Spaghetti Mess ig gl future me

4/20 (2 Hours):
    Fixed fork_from so that 100% works
    Testing resume_from now

4/26 (1.5 Hours):
    wandb resume_from works
    Phase 4 fully complete
    Added extra_snippets which includes a way to directly write the secrets to 
    enviornmental vars (colab compatibility)
    Hmm v5e-1 and v5e-8 feel very similar in speed
    Learn that the data is the bottle neck. I must fix it
    I also learned that my TPU gradient shit is cooked too (l_i_weights + tau is not moving)


4/27 (5 Hours):
    made progress on fixing the dataloaders
    Claude pointed a bunch of problems
    fixed the data loading issues: We shuffle, skip, then shard (but we can't shard after scrambling, so we had to use select)
    Fixed a bunch of tiny errors
    termindation during a checkpoint is to be s
    Moved the Gradient Clipping hook from model.py to the training loop to prevent TPU from not updating learnable params
    Actually just learned that teh problem came from the autocasting making updates that are too small
    Therefore, we need to get rid of casting everything via the enviornmental vars and actually casting them to the right thing
    For now, we'll just cast everything to bf16 (maybe later we'll fix this)

4/29
    Fixed all of it
    Deleted env var for XLA to cast everything to bf16 (since it didn't allow for small updates + large updates at the same time)
    Mixed precision must be acheived in order for the gradient to be unforzen for l_i_weights, tau, and suv_hist
    Added probes to find which mats caused it be fp32
    Casted everything to the correct dtype relative to their inputs
    Implement Pallas Flash Attention for XLA
    Lowered microbatches from 16 -> 8 (OOM) for TPUs. GPUs were doing correct mixed precision the whole time. No changes for GPU profiles
    Graceful Shutdown is cooked. Need fixing
    Data loading works (kinda jank where it pauses literally everything to load a parquet).
    80 seconds = 100 steps, 163840 tokens/sec throughput 🥶🥶🥶
    Our code worked too good: We filled up our runtime with checkpoints before we were able to even upload all of them
    In reality, our sidecar died. 

5/2 (1 Hour):
    Observing crash and proving that termination is not graceful
    Observing behavior after warmup???

5/3 (4 Hours):
    Fixed download_parquet to now properly download the parquet once instead of world_size number of times
    in resume_training - after downloading the checkpoint, delete it
    Learned a ton about Threads, Events, processes, and signals (trying to connect things from class)

    Added heartbeat logic for the train_worker function
     - IPC technique: files on disk (heartbeat.txt)
     - called once every 10 seconds to write something onto disk to ensure thread is alive
     - Start independent thread in each process to write the time
    
    Added checkpoint janitor function (ran by the main thread):
     - After the sidecar finishes uploadeding during graceful exit, we run this
     - Iterate thru all the remaing UPLOADER_REQUEST.json 's in the disk
     - Try to upload if not in there already
     - Timeout mechanism included
    
    In main:
     - Create Threading event (essentially thread-safe bool)
     - graceful shutdown function (1 click = graceful shutdown, 2 clicks = instant shutdown (sig sev))
     - many vars are single-key dicts e.g: {"proc": 0}. The reason is that dicts are mutable container, to prevent race conditions (or rather this is the only way python allows you to do it)
     - created watchdog for sidecar (it's own thread, and it calls the respawn function)
     - created watchdog for the trainer (doesn't respawn, but signals to end)
     - clean shutdown to ensure no processes are orphaned and kill all processes at the end (could take some time)
    
    To replace !python parallel_hardware_trainer.py:
     - Create another script that calls that process instead
     - Wrap it in a loop
     - If user used graceful termination, a temp file gets written to tell the loop to stop
    
    CODE IS NOT TESTED

5/17 (6 hours):
    Code clean-up:
    IPC (Inter-Process Communication)
    Current issue: current logic only terminates the parent process, not the xmp spawn'ed child processes. So when parent dies, they become orphans
    To solve this, use IPC technique of files (like the heartbeat) to send to child processes the graceful shutdown signal
    I reverted Pallas -> DPSA
    Learned !python llm_hardware_univeral_trainer.py does not work, and a new luanching script is required
    Sundays = Good Queue on Kaggle
    Everything == CREEMY!!!

5/18: 
    Starting to fix the router logic
    Our current logic (pre 5/18):
        Router:
            Bilinear projection on hidden state:
            Input: [b, s, hidden_size] (h_s)
            Scanner (q_down_proj): [b, s, hidden_size] * [hidden_size * num_router_latents] = [b, s, num_router_latents] (sc)
            Softmax (sc_s)
            Transpose [b, s, num_router_latents] -> [b, num_router_latents, s] (sc_s_t)
            Project sc_s_t back onto the hidden states: [b,num_router_latents,s] * [b, s, hidden_size] = [b, num_router_latents, hidden_size] (la)
            # Commented out (la) normalizing
            Softmax the l_i weights ??? (they all add up to 1 so they are all .25??? and the threshold is like .5?????)
            Broadcast the l_i onto the latents & sum them:  ([b, num_router_latents, hidden_size] * broadcast [1, num_router_latents, 1]) ->[b, 1, hidden_size] (p_l)
            # Commented out another normalization stage
            Normalize to prep q_up_proj (q_u_p)
            Project p_l onto (q_u_p) [b, 1, hidden_size] * [hidden_size, num_permanent_heads - num_elastic_heads] = [b, 1, num_permanent_heads - num_elastic_heads] (c_s)
            c_s *= tau
            Apply sigmoid activation
            Selection Strategy:
                Calculate topk and threshold
                choose 1 based on warm up
            if use_sigmoid_scaling: multiply the flat mask with the sigmoid scores
            add back the permananent heads (wonder how they'd look, esepecially that they are 1 instead of the others)
            Loss functions:
                Auxilary Loss: to ensure even head is used (Load Balancer)
                    Remember: The scores its looking at are multiplied by tau and passed into sigmoid
                     - Note that the sum of the sigmoid scores can be more than 1 (invariant)
                     - sigmoid essentially ranges between 0.5 - 1
                    For every head, average their scores among the batch (P)
                    Only take the heads that are above the threshold (f)
                    Multiply f and P together for each head and sum the score
                    Multiply by the number of elastic heads and router_aux_loss_coeff
                    If we have 1 head turned on and all all turned off (e.g) .95: (.95*.95) = .9025
                    If we have all x heads (lets say 10) turn on: (.1*.1) * 10 = .1 (This is better)
                    Hmm let me think about this: So the range of the Sigmoid scores are (0 - 0.5) THEY ONLY GO DOWN AND ALL TO ZERO
                    My guess is that because adding heads = more loss, they just turn them all off
                    Maybe I need to make a new loss function so that:
                        - When it uses 3 of the heads (3 + 2), maybe I should not even count the loss from there (like subtract the loss terms, and everything else remain the same)
                        - perhaps even make the loss go even less (like actually insentivise them)
                Sparsity Loss: to ensure the model doesn't pick all the heads at the same time
                    Warm up ramps from 0 - 0.02 within 2000 steps
                    essentially, calculate the mean frequency of each head throughout the batch, and sum the frequencies together
                    I honestly don't think this is a problem.

            
            We could try to pivot to this mechanism:
            Router:
                remove the sigmoid scaling - set it to False
                Rework the warm up:
                    Instead of: topk -> threshold
                    Do: no aux / sparsity loss -> slowly anneal them
                    Pros:
                        the heads actually learn shit
                    Cons:
                        They might need all the heads are important.
            Loss:
                Introduce difficulty based head use / predictions logic
                We need to modify to fine-tune a model to predict the BT_easiness score of a text
                We need to score the bigger text's difficulty
                Clean it up, remove outliers. 
                Now we have text + difficulty values
                Now we need to merge texts with similar scores to make them 1024, 2048, 4096
                    - maybe we take weighted scores
                Clean (remove outliers) and normalize the scores so they are between 0 and 1
                Scores should match the ratio of how many heads the model should use
            Add back the W in AdamW
            Make the weights scale with the total loss when updating .03125 may be pretty aggressive
            
5/30:
    Created a Script to prepare all commonlit BT_easiness Data
    Fine-tuned ModernBERT-base on the BT_easiness Data
    Created Program to test 1024 Samples of a dataset and visualize the difficulty via the logits
    Curating Datasets of mixed difficulty to reach 10 Billion Tokens
    40% Fine web = 4 Billion Tokens
    Goals:
        40% Fine web (5 Billion tokens) (Scientific slightly harder stuff)
        10% Super Easy Stuff (All of Tiny Stories and Simple Stories)
        10% Hard stuff (all of arxiv-abstracts-large, pubmed_simple, and govreport)
        10% TTCW-Based-Review (Harder Literature) (1 Billion Tokens) 
        1% Simple Wiki (All of it)
        25% Reddit

5/31 - 6/5:
    Completed the Following:
        step_1 tokenizes all the datasets into 512 chunks, packs into parquets and makes Repo
        step_2 takes all the repos and evenly distrubute all the data from the datasets and repacks them into parquets
        step_3 uses TPU to perform inference between fine-tuned BT_scorer and data, scoring and merging 512 blocks to become 1024, 2048, 4096 (since bt_scorer had a context window of 512)
    Following Repos were used:
        "JamesResearch1216/ModernBERT-512-pubmed_simple"
        "JamesResearch1216/ModernBERT-512-arxiv-abstracts-large"
        "JamesResearch1216/ModernBERT-512-govreport-summarization"
        "JamesResearch1216/ModernBERT-512-TTCW-Based-Review"
        "JamesResearch1216/ModernBERT-512-fineweb"
        "JamesResearch1216/ModernBERT-512-wikihow-cleaned"
        "JamesResearch1216/ModernBERT-512-simple_wikipedia_LM"
        "JamesResearch1216/ModernBERT-512-reddit-title-body"
        "JamesResearch1216/ModernBERT-512-Amazon_Reviews_Binary_for_Sentiment_Analysis"
        "JamesResearch1216/ModernBERT-512-yelp_review_full"
        "JamesResearch1216/ModernBERT-512-review_corpus"
        "JamesResearch1216/ModernBERT-512-TinyStories"
        "JamesResearch1216/ModernBERT-512-SimpleStories"
    v2 did not have as many easy tokens
    new easier datasets were mixed and added in v3
    Kaggle's default dependencies threw some nasty errors when running the p100 on test-bt-easiness-v5.ipynb. More investigation is needed
    I need to add LR scheduler
    I need to modify the loss function

6/6 - 6/14:
    Added LR scheduler
    Completely redid the loss functions:
        The previous loss functions were designed for softmaxxed activation
            - They relied on total sums, but softmax adds up to 1 whereas sigmoid does not
            - sigmoid was intentionally choosen so that the model can pick multiple heads and have multiple pass the threshold (0.5)
            - however, because they relied on the total sums, and sigmoid values are independent, they are all be driven to 0 and collapse the router
        Therefore, 
        AUX uses a cv^2 (constant * variance ^ 2) head-number invariant 

6/17:
    Pushed to 1.5 Billion tokens
    Currently sitting at 3.2 loss
    Validation is now 0 (need investigation)
    








    
    


Goals:
    create async process to load the parquets without pausing the entire script
    possibly create a drop in replacement for the flex attention via custom triton kernel????
    Fix Router Implementation and Behavior 
    TRAIN DIS SHIIIIII RAWWWHHHHHHHH 😈😈😈😈😈

